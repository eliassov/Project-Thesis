animal_model.R is the univariate model, using the right wing length. The second trait used for bivariate/latent variable models is the beak length. There is a separate file for the bivariate model (bivariate_animal_model.R) and the latent variable model (latent_variable_model.R). All the R files have their corresponding Stan file. The data and output are in their respective folders. 

